<div class="comments inBody">
  {{#each this}}
  <section class="comment">
    <div class="meta">
      {{user.username}} commented on {{format_date created_at}}
    </div>
    <div class="text">
      {{comment_text}}
    </div>
  </section>
  {{/each}}
</div>